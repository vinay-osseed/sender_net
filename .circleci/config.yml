version: 2.1

jobs:
  build:
    docker:
      - image: circleci/php:7.4-node-browsers
    steps:
      - checkout

      - run: sudo apt install wget curl -y
      - run: wget http://pear.php.net/go-pear.phar
      - run: sudo php go-pear.phar -all
      - run: sudo rm -f ./go-pear.phar
      - run: curl -sS https://getcomposer.org/installer | php
      - run: sudo mv ./composer.phar ./composer
      - run: composer require drupal/coder
      - run: sudo ln -s ~/.composer/vendor/bin/phpcs /usr/local/bin
      - run: sudo ln -s ~/.composer/vendor/bin/phpcbf /usr/local/bin
      # - run: sudo pear install PHP_CodeSniffer
      - run: phpcs --config-set installed_paths ./vendor/drupal/coder/coder_sniffer
      - run: which phpcs
      - run: which phpcbf
      - run: phpcs -i
      - run: ls -la
      - run:
          name: "Drupal Coding Standards"
          command: |
            phpcs --standard=Drupal,DrupalPractice --extensions=php,module,inc,install,test,profile,theme,yml ./

workflows:
  1st-Workflow:
    jobs:
      - build
